name: Deploy to GitHub Pages

# Déclenche le workflow sur push vers main
on:
  push:
    branches: [ main ]  # Change en 'master' si ton repo l’utilise
  workflow_dispatch:    # Permet de lancer manuellement depuis l’onglet Actions

# Permissions nécessaires pour GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Évite les déploiements concurrents
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # 🏗️ Étape 1 : Construction du projet
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupère le code du dépôt
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configure Node.js (version 20)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 3️⃣ Installe les dépendances
      - name: Install dependencies
        run: npm ci  # plus fiable que npm install

      # 4️⃣ Construit le projet
      - name: Build project
        run: npm run build

      # 5️⃣ Configure GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4

      # 6️⃣ Téléverse le dossier de build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'  # ⚠️ adapte selon ton framework (ex: 'build' pour React)

  # 🚀 Étape 2 : Déploiement sur GitHub Pages
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build  # Attend que le job 'build' soit terminé

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
